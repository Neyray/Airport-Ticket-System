#ifndef DATABASE_H
#define DATABASE_H

#include <vector>
#include <string>
// 可以在这里引入 Flight.h, User.h 等

class Database {
public:
    Database(); // 构造函数
    // 以后你会在这里添加加载数据、保存数据的方法
    // void load();
    // void save();
};

#endif